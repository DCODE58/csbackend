{
  "name": "felicity-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend proxy that keeps the DeepSeek API key server-side for the Felicity chatbot",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.2.0"
  }
}
